# Suppress deprecation warnings (these are from transitive dependencies and don't affect functionality)
audit-level=moderate
fund=false

# Use latest npm features
legacy-peer-deps=false
prefer-offline=false

